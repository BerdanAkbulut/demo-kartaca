export type ILogin = {
  email: string;
  accessToken: string;
};

export type FormData = {
  email: string;
  password: string;
};

export type ISignUp = {
  email: string;
  password: string;
  firstName: string;
  lastName: string;
};

